# 문장 반복, 재녹음, 입모양 영상 기능 계획

작성일: 2026-06-19

## 1. 목적

사용자가 1-2문장 단위로 말하기 연습을 한 뒤, 녹음된 자기 목소리를 바로 확인하고, 같은 문장을 여러 번 다시 읽으며 변화 과정을 비교할 수 있게 한다. 이후 단계에서는 자유대화/문장 연습 중 전면 카메라로 입모양 영상을 촬영해 발음과 입 움직임을 함께 복습할 수 있게 한다.

사진 메모에서 정리한 핵심 흐름은 다음과 같다.

```text
문장 연습
→ 녹음 완료
→ 끝에서 다시 읽어보기
→ 재녹음 후 다시 확인
→ 발음이 불명확한 단어/문장 반복
→ 입모양 영상까지 함께 확인
```

## 2. 기능 범위

### 2.1 1차 구현: 반복/재녹음

현재 앱 구조에서 바로 구현 가능한 범위다.

- 문장 연습 결과 화면에서 `방금 녹음 듣기` 제공
- 같은 문장을 다시 녹음한 경우 `이전 녹음 듣기` 제공
- `같은 문장 반복` 버튼으로 현재 문장을 유지한 채 재녹음
- 낮은 점수 또는 재시도 횟수를 기반으로 반복 연습 후보 식별
- 녹음 기록 화면에서 기존 `다시 연습` 버튼으로 같은 문장 재진입

### 2.2 2차 구현: 어려운 문장/단어 반복 진입

기존 `PracticeSession`의 `score`, `retryCount`, `contentId`, `targetText`, `audioFilePath`를 활용한다.

- 70점 미만 기록을 `다시 연습 추천`으로 분류
- 같은 `contentId` 또는 같은 `targetText`를 기준으로 반복 횟수 표시
- 오늘의 연습 홈에서 어려운 문장 카드를 노출하고 반복 연습 진입
- 이전 최고 점수와 현재 점수 비교
- 낮은 점수의 단어를 단어 게임/복습 모드로 연결

### 2.3 3차 구현: 입모양 영상 촬영

별도 패키지와 권한이 필요한 기능이며 2026-06-19 기준 1차 범위를 구현했다.

필요 패키지:

```yaml
camera: ^0.12.0+1
video_player: ^2.11.1
```

iOS 권한:

```xml
<key>NSCameraUsageDescription</key>
<string>Speech Rehab uses the camera to record mouth movement during speech practice.</string>
```

Android 권한:

```xml
<uses-permission android:name="android.permission.CAMERA" />
```

권장 흐름:

- 연습 화면에 `입모양 촬영` 토글 추가
- 녹음 시작 시 전면 카메라 무음 영상도 함께 녹화
- 연습 완료 후 `내 목소리 듣기`와 `입모양 영상 보기` 제공
- 녹음 파일과 영상 파일을 같은 `PracticeSession.videoFilePath`에 연결

현재 범위:

- 전면 카메라로 무음 영상 저장
- 연습 직후 하단 시트에서 영상 재생
- 연습 화면에서 촬영 전/중 전면 카메라 프리뷰 표시
- 녹음 보관함에서 과거 세션의 입모양 영상 재생
- 자유대화 화면에서 입모양 촬영 토글, 전면 프리뷰, 마지막 영상 보기 제공
- 자유대화 사용자 메시지에 `mouthVideoPath` 저장
- 세션 삭제 시 연결된 영상 파일 삭제

후속 개선:

- 자유대화 히스토리 목록에서 과거 입모양 영상 다시 열기
- 영상 기반 입술/입 벌림 정도 자동 분석
- 영상 파일 용량 관리와 오래된 영상 정리 정책

## 3. 데이터 모델 확장 방향

현재 `PracticeSession`에는 다음 필드가 이미 있다.

```dart
audioFilePath
score
retryCount
streakCount
previousBestScore
contentId
targetText
mode
```

1차 구현은 기존 history에서 이전 녹음 파일을 찾아 비교한다.

3차 영상 기능에서는 아래 필드를 추가했다.

```dart
final String? videoFilePath;
```

## 4. 구현 순서

1. 결과 화면에서 방금 녹음과 이전 녹음을 나란히 들을 수 있게 한다.
2. 같은 문장 반복 버튼을 명확히 유지하고 재녹음 UX를 개선한다.
3. 녹음 기록 화면의 `다시 연습`을 반복 연습 진입점으로 유지한다.
4. 어려운 문장/단어 반복 진입 카드를 오늘의 연습 홈에 추가한다.
5. 전면 카메라 촬영 권한/서비스를 추가한다.
6. 녹음과 영상 저장 경로를 하나의 세션 기록으로 묶는다.
7. 결과 화면에서 음성 재생과 입모양 영상 재생을 제공한다.

## 5. 현재 반영 상태

2026-06-19 기준 구현 반영:

- `PracticeProgress.previousAudioPath` 추가
- 녹음 완료 시 같은 `contentId` 또는 같은 `targetText`의 이전 녹음 경로를 자동 탐색
- 결과 화면에서 `방금 녹음 듣기`와 `이전 녹음 듣기`를 제공
- 같은 문장 반복은 기존 `같은 문장 반복` 버튼을 유지
- 녹음 보관함의 `다시 연습` 버튼은 기존 세션 기반 재진입으로 유지
- 오늘의 연습 홈에 `어려웠던 문장 다시 읽기` 카드 추가
- 녹음 보관함에 `점수 낮은 순` / `점수 높은 순` / `최신순` 정렬 추가
- 녹음 보관함에 `반복 저장` 버튼과 필터 추가
- 반복 저장 목록은 `SharedPreferences`의 세션 ID 목록으로 보관
- 녹음 보관함에 `재생불가 삭제` 버튼 추가
- 재생불가 삭제는 저장된 오디오 파일 경로가 실제로 존재하지 않는 세션을 일괄 정리
- `camera`, `video_player` 패키지 추가
- iOS/Android 카메라 권한 추가
- `MouthVideoRecorderService` 추가
- `PracticeSession.videoFilePath` 추가
- 연습 화면에 `입모양 촬영` 토글과 결과 영상 보기 추가
- 연습 화면에 전면 카메라 프리뷰 추가
- 녹음 보관함에 입모양 영상 표시/보기 추가
- 자유대화 화면에 입모양 촬영 토글, 전면 프리뷰, 결과 영상 보기 추가
- `ChatMessage.mouthVideoPath` 추가
- 세션 삭제 시 연결된 입모양 영상 파일도 삭제

## 6. 검증 계획

- `flutter analyze`
- `flutter test test/practice_history_service_test.dart test/practice_content_service_test.dart`
- iOS release 빌드에서 녹음 후 `방금 녹음 듣기` 소리 확인
- 같은 문장을 반복 녹음한 뒤 `이전 녹음 듣기` 표시 확인
- 녹음 보관함에서 과거 기록의 `듣기`와 `다시 연습` 확인
- iPad 실제 기기에서 카메라 권한/녹음 동시 사용 테스트 필요
